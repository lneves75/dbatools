#!/bin/bash

# This script will prepare a database for load tests
CONN_STR="-U app -h dbmaster"

psql $CONN_STR -c "drop database if exists dh_live_backend" postgres
psql $CONN_STR -c "create database dh_live_backend with owner app" postgres
pgbench $CONN_STR -i -s 10 dh_live_backend
